node_modules/
dist/
target/
out/
build/
.env
*.log
.DS_Store
.wrangler/

# Internal-patterns research brief: documents private infra (hayabusa backend
# URLs, Cloudflare resource ids) that is deliberately gitignored elsewhere.
# Kept on disk for reference, never committed.
docs/research/local-patterns.md

# TypeScript incremental build info
*.tsbuildinfo

# Wrangler local state
.wrangler/
